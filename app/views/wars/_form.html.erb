<%= form_for(@war) do |f| %>

  <div class="field">
    Opposiing Clan: <%= f.text_field :opponent %>
  </div>

  <div class="actions">
    <%= f.submit "Create War", class: "btn btn-success" %>
  </div>
<% end %>
