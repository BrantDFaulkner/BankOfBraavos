<%= form_for(@violation_type) do |f| %>
  <% if @violation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@violation_type.errors.count, "error") %> prohibited this violation_type from being saved:</h2>

      <ul>
      <% @violation_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
